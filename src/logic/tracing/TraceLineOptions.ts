import type { Color } from "@/model/Color";

export interface TraceLineOptions {
    drawingColor: Color;
    hintColor: Color;
    fieldWidth: number;
    fieldHeight: number;
    lineWidth: number;
}